{
  "name": "chainsentinel-mcp-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "ChainSentinel MCP tool server — exactly 11 read-only tools over the REST API (stdio transport)",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/index.js",
    "smoke": "node scripts/smoke.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.12.0",
    "zod": "3.23.8"
  }
}
